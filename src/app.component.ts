import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div>
      <!-- The demo UI is loaded via the index.html file -->
      <!-- This component serves as a placeholder for Angular functionality -->
    </div>
  `,
  styles: []
})
export class AppComponent {
  title = 'FinchesEye Animation Library Demo';
}
